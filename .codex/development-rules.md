# Codex Development Rules Adapter

Canonical file: `.agents/development-rules.md`.

Codex should read `AGENTS.md` first, then `.agents/development-rules.md`. This file exists only so older prompts that mention `.codex/development-rules.md` still have a stable pointer.
